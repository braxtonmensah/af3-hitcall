# Handoff: AlphaFold faculty-sponsor research + Jev (2026-09-22)

Read this first. It is the complete state of the project as of the evening of 2026-09-22.

## UPDATE 2026-09-22 ~19:50 (supersedes sections 4, 5 status and 7.1)

- **Section 4 jobs finished and recorded in RESULTS.md.** CLINVAR2: positive control failed again
  (precedented OR 1.31, CI 0.93-1.83); not interpreted; the disease-variant route is closed. LITJEV:
  no signal (L1 +0.004, CI -0.010..0.020), and the instrument is insensitive (precedented reference
  only 1.8%). Neither goes in the pitch.
- **analyze.py now scores TfoS** (Amendment 2 -> `tfos_results.json`) and had a real bug fixed: zips
  with a nested folder made every job name wrong and all results silently dropped. Verified end to
  end on synthetic zips (85/85 + 10/10 parsed, planted ChiS-TfoS recovered at rank 1).
  `jobs/chain_counts.json` now includes the 10 `vc_tfos_*` jobs.
- **AF Server at 19:45:** 27 unique jobs completed (002-005, 007-022, 024-030), **006 and 023 FAILED**
  (server shows only "Failed"; the pools look normal, so resubmit them), 001 still a draft. Remaining
  quota today: 2. Left to submit: 001, 006, 023, 031-085, tfos 01-10 = **68 jobs**.
- Chrome extension kept timing out on the AF Server tab this session.

## 1. Goal and ground rules

- **Goal:** get an IU faculty sponsor (target: **Ankur Dalia**, Biology, ankdalia@iu.edu) so Braxton
  can use **Big Red 200**. The pitch is: "your genome-wide AlphaFold3 interaction screens are
  reliable for X and not for Y; here are the numbers; I can help you call hits."
- **Standing instruction (active /goal):** research and test hypotheses; **send no emails**; get
  good results first.
- **Method (keep it):** every test is pre-registered in a `PREREG_*.md` committed *before* the data
  is joined. `git log --oneline` in this repo shows the order, and that order is the credibility.
  Post-hoc work is labelled as such (`POSTHOC.md`, `posthoc_*.py`).
- Writing for Braxton: no em dashes, short, plain.

## 2. Where things live

| What | Path |
|---|---|
| Code, pre-registrations, results (git) | `NQ_Project/af3-hitcall/` |
| Write-up of every result | `af3-hitcall/RESULTS.md` |
| Raw data (not in OneDrive, C: has ~17 GB free) | `C:\Users\bmens\NQ_local\af3-hitcall\` (`data/`, `suppl/`, `burke/`, `vibrio/`, `xlms2020/`, `oreilly/`) |
| Vibrio pilot | `af3-hitcall/vibrio_pilot/` (README = runbook) |
| Jev code | `NQ_Project/jev/` (not git) |
| Memory notes | `af3-hitcall-audit`, `iu-alphafold-faculty-sponsor`, `jev-playground` |

## 3. Results so far (details and CIs in RESULTS.md)

**Headline:** AlphaFold interaction screens are excellent on complexes with a solved structural
precedent and weak on never-solved interactions. Published accuracy averages the two.

| Truth set | Precedented | Never solved |
|---|---|---|
| *M. genitalium* pooled AF3 vs STRING (Todor 2026) | 0.85 | 0.71 |
| In-cell crosslinks, *M. pneumoniae* (O'Reilly 2020) | 0.95 | 0.57 |
| Human AF2/FoldDock vs random pairs (Burke 2023) | 0.92 | 0.64 |

- *B. subtilis* / AF-Multimer recall replicates (53% vs 21%).
- **Time split:** complexes solved only after AF3's cutoff still score 0.81, so this is stability,
  not memorisation (just past the pre-registered bar).
- **Rejected ideas:** per-protein z-normalisation (H2), interface PAE vs ipTM (H3).
- **Post hoc:** the paper's pool-size figure is confounded (disjoint protein sections), and 4k pools
  equal 5k on matched pairs. A second pool replicate adds only +0.016. A bait's #1 hit is right 81%
  of the time if precedented vs 10% if never-solved.
- **Self-caught error:** the original precedent rule counted multi-entity homomers (6OJY) as
  co-complexes. The strict rule (`precedent_matrix_strict`) is now used, and H1 was unaffected.
- **HH2 (crosslinks on confident human models):** inconclusive (+0.14, CI -0.08..0.30).
- **CLINVAR v1:** failed its positive control. It compared interfaces against the whole protein,
  and core burial dominates. v2 (surface-matched) is pre-registered; see section 4.

**Practical facts for the pitch:** AF3 fits 4,352 tokens on an A100 40 GB (Big Red 200's GPU),
5,120 on 80 GB, and 1,280 on a V100 (Quartz). So Quartz can't run useful pools, Big Red 200 can run
4k pools, and 4k equals 5k.

## 4. Jobs that were running (now FINISHED, results in RESULTS.md)

- CLINVAR v2: positive control failed narrowly (precedented OR 1.31 [0.93, 1.83]), so not interpreted.
  The novel OR of 1.69 [1.32, 2.16] is descriptive only. A v3 would need more power (e.g. all HuRI and
  hu.MAP confident models plus ClinVar-only variants, or a Mantel-Haenszel per-protein design), and a
  new pre-registration.
- LITJEV: floor effect (lit-direct at most 1.8% in every group), uninformative. Don't rerun as designed.
  A better design: restrict to pairs with a curated low-throughput interaction record (IntAct/BioGRID
  low-throughput) as truth, not abstract co-mention.

(Original instructions kept below for reference.)

1. **CLINVAR v2 extraction** -> `burke/interfaces_v2.jsonl` (target about 10,240 lines). If it died,
   rerun `python human_extract.py 10 interfaces_v2.jsonl` (it skips done items). Then run
   `python analysis_clinvar2.py` (PREREG_CLINVAR2: T2 positive control first).
2. **LITJEV** (Jev reads abstracts for 850 sampled human pairs) -> `burke/litjev.jsonl`. Resume from
   `NQ_Project/jev` with `npx tsx ../af3-hitcall/jev_lit_human.mts`, then `python analysis_litjev.py`.
   L1 = among co-mentioned never-solved pairs, P(best >= 0.8 | confident) - P(... | low), with a
   bootstrap CI (script written before results).

## 5. Vibrio pilot (the deciding experiment, in Dalia's organism)

- Pre-registered in `PREREG_VIBRIO.md` (plus Amendment 1: strict classes; Amendment 2: TfoS extension).
- 24 competence proteins + 60 decoys, 85 pools at a 4k-aa cap. Positives: never-solved PilT-PilU,
  CBP-ChiS, DprA-ComM, PilM-N, N-O, O-P; precedented PilP-PilQ, PilB-PilC, PilT-PilC. Extension:
  ChiS-TfoS (TfoS = VC_2080), 10 jobs in `jobs/afserver_tfos.json`.
- **AF Server status:** jobs **002 to 030 submitted** on 2026-09-22 (019 went in twice; harmless,
  because analysis dedups by name). **Still to submit:** draft `vc_pilot_001`, `afserver_batch02.json`
  (031-060), `afserver_batch03.json` (061-085), `afserver_tfos.json`: 66 jobs at 30/day, so 3 days.
- **AF Server gotchas (learned the hard way):**
  - Upload the JSON through the hidden file input (patch `HTMLInputElement.prototype.click`, then
    `file_upload`).
  - Per job: open the draft -> wait until the entity count equals the expected chain count
    (`jobs/chain_counts.json`) -> **real click** Continue -> check that the dialog name and the first
    protein length match -> **real click** Confirm. Scripted `.click()` on Confirm is unreliable.
  - The dialog's name field is often stale (shows the previous job); fix it before confirming.
  - Launches take 30-60 s to register ("Job launch pending"). Never re-open a draft until the quota
    drops, or it will be submitted twice.
  - Set history to 100 rows per page or drafts are invisible.
- **Scoring:** download every `fold_vc_pilot_XXX.zip` into one folder, then
  `python vibrio_pilot/analyze.py <folder>`. It rejects any result whose chain count doesn't match
  its pool. **TODO:** `analyze.py` does not read `pools_tfos.csv` yet; extend it to score ChiS-TfoS
  against the main pilot's decoy-decoy null (Amendment 2).
- **Jev literature table** (descriptive): recovered all 3 pre-registered never-solved positives, found
  ChiS-TfoS, and made 1 false positive (CBP-TfoS). See the vibrio_pilot README.

## 6. Jev (TypeSafe System One model)

- **Key situation:** the working key is a **reseller** key (`jv_live_...`, jevtypesafeai.com,
  `POST /api/v1/decide`, about $4.5 left) in `jev/.env` as `JEV_API_KEY`. The official API rejects it
  (401). **Braxton was upset about being on a reseller.** He must click "Continue with Google" at
  console.typesafe.ai himself (account creation and terms are his). Then: create the official key ->
  `TYPESAFE_API_KEY` in `jev/.env` -> remove `JEV_API_KEY` -> revoke the reseller key. He also
  pasted the reseller key in chat, so rotate it.
- **Reseller limits:** max 50 questions per request; choice `criteria` must be a label->description
  map.
- **Built and verified:**
  - `src/jev.ts`: one client, auto-picks reseller or official.
  - The playground (`npm start`, port 5173), rewired and verified.
  - `src/mcp.ts`: zero-dependency MCP server (`jev_decide/noul/choice`), tested over stdio. Enable:
    `claude mcp add jev -- npx -y tsx "<path>/jev/src/mcp.ts"`.
  - `src/memory_router.ts` -> `dist/memory_router.mjs`: a UserPromptSubmit hook where ONE Jev choice
    over all MEMORY.md entries picks the relevant memory files. Tested: 5/5 prompts routed right,
    "none" for off-topic, ~1 s Jev / ~2.8 s total, $0.0025 per prompt reseller ($0.00025 official).
    **Not enabled** (it changes settings; ask first). Enable by adding to `~/.claude/settings.json`:
    `"hooks": {"UserPromptSubmit": [{"hooks": [{"type": "command", "command": "node \"C:/Users/bmens/OneDrive - Indiana University/NQ_Project/jev/dist/memory_router.mjs\"", "timeout": 10}]}]}`
  - Literature judge: `vibrio_pilot/lit_evidence.mts`, `jev_lit_human.mts` (`.mts` needed outside
    the jev package).
- **Don't use Jev for:** Mercor Chariot (no-LLM rule), graded coursework, anything numeric (it
  can't count or do math).

## 7. Next steps, in order

1. Finish CLINVAR v2 and LITJEV (section 4); record them in RESULTS.md whatever the outcome.
2. Submit the remaining 66 AF Server jobs over 3 days; download; score with `analyze.py`; add the TfoS
   scoring.
3. Official Jev key once Braxton signs in.
4. Only then: draft the Dalia email (**do not send**; Braxton sends). Lead with: the three-organism
   precedent result, the 4k-pool / Big Red 200 point, and the pilot outcome in his own organism.
5. Before posting anything publicly, re-check prior art (see "Prior-art check" in RESULTS.md) and
   cite Todor 2026, Burke 2023, O'Reilly 2020/2023, and Abramson 2024 (AF3 Output Terms:
   non-commercial).

## 8. Honest framing

This is a solid methods audit and a strong undergraduate pitch, not a biological breakthrough. The
breakthrough path is a new *Vibrio* interaction from the pilot that Dalia's lab confirms at the bench.
