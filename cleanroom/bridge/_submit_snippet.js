window.S=async n=>{const b=[...document.querySelectorAll('input')].find(i=>/search/i.test(i.placeholder||i.ariaLabel||''));Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value').set.call(b,n);b.dispatchEvent(new Event('input',{bubbles:1}));await new Promise(r=>setTimeout(r,1500));const t=[...document.querySelectorAll('td')].find(e=>e.innerText.trim()===n);if(!t)return{n,ok:0,w:'nf'};if(!/edit_document/.test(t.closest('tr').innerText))return{n,ok:0,w:'nd'};t.click();let c=null;for(let i=0;i<10;i++){await new Promise(r=>setTimeout(r,700));c=[...document.querySelectorAll('button')].find(x=>/^Continue and preview job$/i.test(x.innerText.trim()));if(c)break;}if(!c)return{n,ok:0,w:'nc'};c.click();for(let i=0;i<10;i++){await new Promise(r=>setTimeout(r,700));const f=[...document.querySelectorAll('button')].find(x=>/^Confirm and submit job$/i.test(x.innerText.trim()));if(f){f.click();await new Promise(r=>setTimeout(r,900));return{n,ok:1};}}return{n,ok:0,w:'nf2'};};